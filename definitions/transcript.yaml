paths:
  /schools:
    post:
      tags:
        - school
      summary: Create a new instance of the model and persist it into the data source.
      operationId: school.create
      parameters:
        - name: data
          in: body
          description: Model instance data
          required: false
          schema:
            $ref: '#/definitions/school'
      responses:
        '200':
          description: Request was successful
          schema:
            $ref: '#/definitions/school'
      deprecated: false
    put:
      tags:
        - school
      summary: Update an existing model instance or insert a new one into the data source.
      operationId: school.upsert
      parameters:
        - name: data
          in: body
          description: Model instance data
          required: false
          schema:
            $ref: '#/definitions/school'
      responses:
        '200':
          description: Request was successful
          schema:
            $ref: '#/definitions/school'
      deprecated: false
    get:
      tags:
        - school
      summary: Find all instances of the model matched by filter from the data source.
      operationId: school.find
      parameters:
        - name: filter
          in: query
          description: 'Filter defining fields, where, include, order, offset, and limit'
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: array
            items:
              $ref: '#/definitions/school'
      deprecated: false
  '/schools/{id}/exists':
    get:
      tags:
        - school
      summary: Check whether a model instance exists in the data source.
      operationId: 'school.exists__get_schools_{id}_exists'
      parameters:
        - name: id
          in: path
          description: Model id
          required: true
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: object
            properties:
              exists:
                type: boolean
      deprecated: false
  '/schools/{id}':
    head:
      tags:
        - school
      summary: Check whether a model instance exists in the data source.
      operationId: 'school.exists__head_schools_{id}'
      parameters:
        - name: id
          in: path
          description: Model id
          required: true
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: object
            properties:
              exists:
                type: boolean
      deprecated: false
    get:
      tags:
        - school
      summary: Find a model instance by id from the data source.
      operationId: school.findById
      parameters:
        - name: id
          in: path
          description: Model id
          required: true
          type: string
          format: JSON
        - name: filter
          in: query
          description: Filter defining fields and include
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            $ref: '#/definitions/school'
      deprecated: false
    delete:
      tags:
        - school
      summary: Delete a model instance by id from the data source.
      operationId: school.deleteById
      parameters:
        - name: id
          in: path
          description: Model id
          required: true
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: object
      deprecated: false
    put:
      tags:
        - school
      summary: Update attributes for a model instance and persist it into the data source.
      operationId: school.prototype.updateAttributes
      parameters:
        - name: data
          in: body
          description: An object of model property name/value pairs
          required: false
          schema:
            $ref: '#/definitions/school'
        - name: id
          in: path
          description: PersistedModel id
          required: true
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            $ref: '#/definitions/school'
      deprecated: false
  /schools/findOne:
    get:
      tags:
        - school
      summary: Find first instance of the model matched by filter from the data source.
      operationId: school.findOne
      parameters:
        - name: filter
          in: query
          description: 'Filter defining fields, where, include, order, offset, and limit'
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            $ref: '#/definitions/school'
      deprecated: false
  /schools/update:
    post:
      tags:
        - school
      summary: Update instances of the model matched by where from the data source.
      operationId: school.updateAll
      parameters:
        - name: where
          in: query
          description: Criteria to match model instances
          required: false
          type: string
          format: JSON
        - name: data
          in: body
          description: An object of model property name/value pairs
          required: false
          schema:
            $ref: '#/definitions/school'
      responses:
        '200':
          description: Request was successful
          schema:
            description: The number of instances updated
            type: object
      deprecated: false
  /schools/count:
    get:
      tags:
        - school
      summary: Count instances of the model matched by where from the data source.
      operationId: school.count
      parameters:
        - name: where
          in: query
          description: Criteria to match model instances
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: object
            properties:
              count:
                type: number
                format: double
      deprecated: false
  /schools/change-stream:
    post:
      tags:
        - school
      summary: Create a change stream.
      operationId: school.createChangeStream__post_schools_change-stream
      parameters:
        - name: options
          in: formData
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: file
      deprecated: false
    get:
      tags:
        - school
      summary: Create a change stream.
      operationId: school.createChangeStream__get_schools_change-stream
      parameters:
        - name: options
          in: query
          required: false
          type: string
          format: JSON
      responses:
        '200':
          description: Request was successful
          schema:
            type: file
      deprecated: false
definitions:
  school:
    properties:
      id:
        type: number
        format: double
      name:
        type: string
    required:
      - name
    additionalProperties: false
  x-any:
    properties: {}
swagger: '2.0'
info:
  x-ibm-name: transcript
  version: 1.0.0
  title: transcript
schemes:
  - https
host: $(catalog.host)
basePath: /api
consumes:
  - application/json
produces:
  - application/json
securityDefinitions:
  clientIdHeader:
    type: apiKey
    in: header
    name: X-IBM-Client-Id
  clientSecretHeader:
    in: header
    name: X-IBM-Client-Secret
    type: apiKey
security:
  - clientIdHeader: []
    clientSecretHeader: []
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  catalogs:
    apic-dev:
      properties:
        runtime-url: $(TARGET_URL)
    sb:
      properties:
        runtime-url: 'http://localhost:4001'
  assembly:
    execute:
      - invoke:
          target-url: $(runtime-url)$(request.path)$(request.search)
